# ---------- Base (shared by scripts/tools) ----------
# Supabase (local or cloud)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=replace_me
SUPABASE_ANON_KEY=replace_me
# Some Supabase UIs refer to "publishable key" (same as anon)
SUPABASE_PUBLISHABLE_KEY=${SUPABASE_ANON_KEY}

# OpenAI (server-only; used by tools/jobs)
OPENAI_API_KEY=replace_me

# Jobs/HMAC (used by admin job endpoints)
HMAC_JOB_SECRET=replace_me

# Testkit (server-only test helpers)
TESTKIT_ON=false
TESTKIT_TOKEN=replace_me

# Base URL the web app will run on (local dev default)
BASE_URL=http://localhost:3000

# Optional: Sentry / Analytics
SENTRY_DSN=
