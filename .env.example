# -------------------------------------------------------------------
# ROOT .env.example  (for scripts/tools, not for Next.js client code)
# -------------------------------------------------------------------
# Copy this to a real environment file as needed:
# - .env.local (local dev with Supabase Docker)
# - .env.dev   (cloud development)
# - .env.prod  (production)
#
# IMPORTANT:
# - Never commit real secrets. Only .example files are tracked.
# - Server-only secrets DO NOT have NEXT_PUBLIC_ prefix.
# - Client-exposed variables belong in /web/.env.* (handled later).
# -------------------------------------------------------------------

# --- Supabase (server-side & tooling) ---
# URL of your Supabase project (local or cloud)
SUPABASE_URL=

# Service-Role key (server-only). DO NOT expose to client.
SUPABASE_SERVICE_ROLE_KEY=

# "Anon" (publishable) key. Safe for server-side use; never expose directly to client via root.
# (For client exposure, use NEXT_PUBLIC_SUPABASE_ANON_KEY inside /web/.env.*)
SUPABASE_ANON_KEY=

# Some Supabase UIs call this "Publishable Key". If your project has it, keep for completeness.
SUPABASE_PUBLISHABLE_KEY=

# --- OpenAI (server-side tooling / generation scripts) ---
OPENAI_API_KEY=

# --- Testkit (non-prod seeding + E2E tooling) ---
# Turn on in local/dev envs only. Keep OFF for prod.
TESTKIT_ON=false
TESTKIT_TOKEN=

# --- App base URL for scripts / Playwright ---
BASE_URL=http://localhost:3000

# --- CI flag for scripts that behave differently under CI ---
CI=false
