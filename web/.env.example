# =============================================================================
# INFRASTRUCTURE & SECRETS (Keep in Environment Variables)
# =============================================================================

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Stripe Configuration
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PRICE_ID=price_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Email Configuration (Resend)
RESEND_API_KEY=your_resend_api_key
FROM_EMAIL=no-reply@example.com

# Google Authentication
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
NEXT_PUBLIC_GOOGLE_ONE_TAP=1

# Sentry Error Tracking
SENTRY_DSN=your_sentry_dsn
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Application URLs and Configuration
NEXT_PUBLIC_APP_BASE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=https://permit-school.com
APP_BASE_URL=http://localhost:3000

# Billing URLs
BILLING_SUCCESS_URL=http://localhost:3000/billing?status=success
BILLING_CANCEL_URL=http://localhost:3000/billing?status=cancel

# Build and Version Information
NEXT_PUBLIC_ENV=dev
NEXT_PUBLIC_COMMIT_SHA=dev
NEXT_PUBLIC_BUILD_AT=

# Offline Development
OFFLINE_DEV=0

# Test Configuration
TESTKIT_TOKEN=your_testkit_token
TESTKIT_ON=false
TEST_ADMIN_USER=
TEST_STUDENT_USER=
TEST_MINOR_USER=
BASE_URL=http://localhost:3000

# CI/CD Configuration
CI=false

# =============================================================================
# JURISDICTION-SPECIFIC CONFIGURATION (Moved to Database)
# =============================================================================
# The following items are now stored in the jurisdiction_configs table
# and should be configured via the admin interface at /admin/jurisdictions
#
# - Final exam questions, pass percentage, seat time requirements
# - Certificate issuer name, license, and prefix
# - Support email and phone numbers
# - Regulatory signing secrets
# - Fulfillment thresholds
# - Terms, privacy, and disclaimer text
#
# Environment variables below are kept as fallbacks for backward compatibility
# but should be configured in the database for multi-state support.
# =============================================================================

# Final Exam Configuration (Fallback - use database config instead)
FINAL_EXAM_NUM_QUESTIONS=30
FINAL_EXAM_PASS_PCT=0.8
FINAL_EXAM_MINUTES_REQUIRED=150

# Certificate Configuration (Fallback - use database config instead)
APP_ORIGIN=http://localhost:3000
CERT_ISSUER_NAME=Acme Driving Academy
CERT_ISSUER_LICENSE=CA-INS-000123
CERT_NUMBER_PREFIX=CA

# Support Information (Fallback - use database config instead)
SUPPORT_EMAIL=support@example.com
NEXT_PUBLIC_SUPPORT_EMAIL=support@example.com
NEXT_PUBLIC_SUPPORT_PHONE=1-800-PERMIT

# Regulatory and Compliance (Fallback - use database config instead)
REGULATORY_SIGNING_SECRET=your_regulatory_signing_secret

# Rate Limiting
RATE_LIMIT_MAX=60

# Fulfillment Configuration (Fallback - use database config instead)
NEXT_PUBLIC_FULFILLMENT_LOW_STOCK_THRESHOLD=200
