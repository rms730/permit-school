# =============================================================
# PERMIT SCHOOL - ENVIRONMENT VARIABLES
# =============================================================
# 
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local
#
# =============================================================
# REQUIRED: INFRASTRUCTURE & SECRETS
# =============================================================

# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Stripe Configuration (Required for billing)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Email Configuration (Required for notifications)
RESEND_API_KEY=your_resend_api_key
FROM_EMAIL=no-reply@example.com

# Google Authentication (Required for sign-in)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
NEXT_PUBLIC_GOOGLE_ONE_TAP=1

# =============================================================
# OPTIONAL: APPLICATION CONFIGURATION
# =============================================================

# Application URLs
NEXT_PUBLIC_APP_BASE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=https://permit-school.com
APP_BASE_URL=http://localhost:3000

# Sentry Error Tracking (Optional)
SENTRY_DSN=your_sentry_dsn
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Build and Version Information (Auto-populated in CI)
NEXT_PUBLIC_ENV=dev
NEXT_PUBLIC_COMMIT_SHA=dev
NEXT_PUBLIC_BUILD_AT=

# Development Configuration
OFFLINE_DEV=0
RATE_LIMIT_MAX=60

# =============================================================
# TESTING & CI/CD CONFIGURATION
# =============================================================

# Test Configuration (Optional - for E2E tests)
TESTKIT_TOKEN=your_testkit_token
TESTKIT_ON=false
TEST_ADMIN_USER=
TEST_STUDENT_USER=
TEST_MINOR_USER=
BASE_URL=http://localhost:3000

# CI/CD Configuration (Auto-set in CI)
CI=false

# =============================================================
# DEPRECATED: JURISDICTION-SPECIFIC CONFIGURATION
# =============================================================
# 
# The following variables have been moved to the database and are
# kept only for backward compatibility. They should be configured
# via the admin interface at /admin/jurisdictions instead.
#
# These will be removed in a future version.

# Final Exam Configuration (Use database config instead)
FINAL_EXAM_NUM_QUESTIONS=30
FINAL_EXAM_PASS_PCT=0.8
FINAL_EXAM_MINUTES_REQUIRED=150

# Certificate Configuration (Use database config instead)
CERT_ISSUER_NAME=Acme Driving Academy
CERT_ISSUER_LICENSE=CA-INS-000123
CERT_NUMBER_PREFIX=CA

# Support Information (Use database config instead)
SUPPORT_EMAIL=support@example.com
NEXT_PUBLIC_SUPPORT_EMAIL=support@example.com
NEXT_PUBLIC_SUPPORT_PHONE=1-800-PERMIT

# Regulatory Configuration (Use database config instead)
REGULATORY_SIGNING_SECRET=your_regulatory_signing_secret

# Fulfillment Configuration (Use database config instead)
NEXT_PUBLIC_FULFILLMENT_LOW_STOCK_THRESHOLD=200
